---
import BaseLayout from "../layouts/BaseLayout.astro";
import Hero from "../components/Hero.astro";
import ServicesGrid from "../components/ServicesGrid.astro";
import Testimonials from "../components/Testimonials.astro";
import ContactCTA from "../components/ContactCTA.astro";

// Generate initial video URL with default dimensions
function generateVideoUrl(width = 1800, height = 1400) {
  return `https://res.cloudinary.com/dfhs35j6e/video/upload/q_auto:best,fps_24,du_15/v1752600931/DJI_Fly_Video_Jul_13_2025_xaawxu.mp4`;
}

const initialVideoUrl = generateVideoUrl();
---

<BaseLayout
  title="Professional Drone Videography Services | NJ Drones"
  description="Professional drone videography services for real estate, weddings, automotive, and business promotions in New Jersey and surrounding areas. Get stunning aerial footage that captures your vision."
>
  <Hero
    title="Elevate Your Story with Professional Drone Videography"
    subtitle="Capturing breathtaking aerial footage for real estate, weddings, automotive, and business promotions across New Jersey and beyond."
    videoSrc={initialVideoUrl}
    imageSrc="/images/hero-background.jpg"
  />

  <ServicesGrid />

  <section class="section-padding bg-white">
    <div class="container-max">
      <div class="text-center mb-16">
        <h2 class="text-3xl md:text-4xl font-bold text-gray-900 mb-4">
          Why Choose NJ Drones?
        </h2>
        <p class="text-xl text-gray-600 max-w-2xl mx-auto">
          We combine cutting-edge technology with creative vision to deliver
          exceptional results
        </p>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8">
        <div class="text-center">
          <div
            class="bg-primary-100 rounded-full w-16 h-16 flex items-center justify-center mx-auto mb-4"
          >
            <svg
              class="w-8 h-8 text-primary-600"
              fill="none"
              stroke="currentColor"
              viewBox="0 0 24 24"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
            </svg>
          </div>
          <h3 class="text-xl font-semibold text-gray-900 mb-2">
            FAA Certified
          </h3>
          <p class="text-gray-600">
            Licensed and insured drone pilot with all necessary certifications
          </p>
        </div>

        <div class="text-center">
          <div
            class="bg-primary-100 rounded-full w-16 h-16 flex items-center justify-center mx-auto mb-4"
          >
            <svg
              class="w-8 h-8 text-primary-600"
              fill="none"
              stroke="currentColor"
              viewBox="0 0 24 24"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M15 10l4.553-2.276A1 1 0 0121 8.618v6.764a1 1 0 01-1.447.894L15 14M5 18h8a2 2 0 002-2V8a2 2 0 00-2-2H5a2 2 0 00-2 2v8a2 2 0 002 2z"
              ></path>
            </svg>
          </div>
          <h3 class="text-xl font-semibold text-gray-900 mb-2">
            4K Video Quality
          </h3>
          <p class="text-gray-600">
            Professional-grade equipment delivering stunning 4K resolution
            footage
          </p>
        </div>

        <div class="text-center">
          <div
            class="bg-primary-100 rounded-full w-16 h-16 flex items-center justify-center mx-auto mb-4"
          >
            <svg
              class="w-8 h-8 text-primary-600"
              fill="none"
              stroke="currentColor"
              viewBox="0 0 24 24"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path>
            </svg>
          </div>
          <h3 class="text-xl font-semibold text-gray-900 mb-2">
            Fast Turnaround
          </h3>
          <p class="text-gray-600">
            Quick delivery of edited footage within 48-72 hours
          </p>
        </div>

        <div class="text-center">
          <div
            class="bg-primary-100 rounded-full w-16 h-16 flex items-center justify-center mx-auto mb-4"
          >
            <svg
              class="w-8 h-8 text-primary-600"
              fill="none"
              stroke="currentColor"
              viewBox="0 0 24 24"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M4.318 6.318a4.5 4.5 0 000 6.364L12 20.364l7.682-7.682a4.5 4.5 0 00-6.364-6.364L12 7.636l-1.318-1.318a4.5 4.5 0 00-6.364 0z"
              ></path>
            </svg>
          </div>
          <h3 class="text-xl font-semibold text-gray-900 mb-2">
            Personal Service
          </h3>
          <p class="text-gray-600">
            Dedicated support from consultation to final delivery
          </p>
        </div>
      </div>
    </div>
  </section>

  <Testimonials />

  <ContactCTA />
</BaseLayout>

<!-- <script>
  // Window dimensions tracking
  let windowWidth = window.innerWidth;
  let windowHeight = window.innerHeight;

  // Generate video URL with current dimensions
  function generateVideoUrl() {
    return `https://res.cloudinary.com/dfhs35j6e/video/upload/q_auto:best,fps_24,du_15,w_${windowWidth},h_${windowHeight}/v1752600931/DJI_Fly_Video_Jul_13_2025_xaawxu.mp4`;
  }

  // Update video source with new dimensions
  function updateVideoSource() {
    const videoElement = document.querySelector(
      "section video"
    ) as HTMLVideoElement;
    if (videoElement) {
      const newVideoUrl = generateVideoUrl();
      const sources = videoElement.querySelectorAll("source");

      sources.forEach((source) => {
        source.src = newVideoUrl;
      });

      // Reload the video with new source
      videoElement.load();

      console.log("Video updated with dimensions:", {
        width: windowWidth,
        height: windowHeight,
        url: newVideoUrl,
      });
    }
  }

  // Update dimensions on resize
  function updateWindowDimensions() {
    windowWidth = window.innerWidth;
    windowHeight = window.innerHeight;

    // Update video source with new dimensions
    updateVideoSource();

    // Dispatch custom events when dimensions change
    window.dispatchEvent(
      new CustomEvent("windowResize", {
        detail: { width: windowWidth, height: windowHeight },
      })
    );
  }

  // Initialize on page load
  document.addEventListener("DOMContentLoaded", () => {
    updateWindowDimensions();

    // Set initial video source
    updateVideoSource();

    // Listen for window resize events
    window.addEventListener("resize", updateWindowDimensions);

    // Optional: Log dimensions for debugging
    console.log("Window dimensions:", {
      width: windowWidth,
      height: windowHeight,
    });
  });

  // Optional: Listen for custom resize events
  window.addEventListener("windowResize", (event) => {
    console.log("Window resized:", (event as CustomEvent).detail);
  });
</script> -->
